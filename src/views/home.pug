extends base.pug
include mixin/video.pug


block content
    a(href="videos/upload") Video upload &rarr;
    ul 
        each list in videos
            +video(list) 
        else 
            li 
                span Nothing else!