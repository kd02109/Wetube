extends base.pug
include mixin/video.pug


block content
    div.video-grid
            each list in videos
                +video(list) 
            else 
                span.empty__message Nothing else!