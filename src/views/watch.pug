extends base.pug

block content 
    p=video.description
    small=video.createdAt
    div 
        a(href=`${video.id}/edit`) Edit video &rarr;
        br
        a(href=`${video.id}/delet`) Delete video &rarr;